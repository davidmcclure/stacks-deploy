---

- name: Create a filesystem
  become: yes
  ignore_errors: yes
  filesystem:
    fstype: ext4
    dev: /dev/xvdf

- name: Mount the data volume
  become: yes
  ignore_errors: yes
  mount:
    fstype: ext4
    src: /dev/xvdf
    name: /data
    state: mounted
