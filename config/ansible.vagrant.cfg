
[defaults]

vault_password_file = ~/.stacks-pw.txt
forks = 50

deprecation_warnings = False
host_key_checking = False

hostfile = .vagrant/inventory

[ssh_connection]
ssh_args = -o ForwardAgent=yes -o UserKnownHostsFile=/dev/null
