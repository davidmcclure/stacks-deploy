
server {

  listen 80;
  server_name {{ item.server_name }};

  location /static/ {
    root {{ item.static_root }};
  }

  location / {
    include proxy_params;
    proxy_pass http://unix:{{ item.socket }};
  }

}
